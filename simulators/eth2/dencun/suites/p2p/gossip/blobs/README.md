# Dencun Interop Blob P2P Gossip Suite

To run locally use:

```bash
./hive --client-file ./configs/cancun.yaml --client "go-ethereum,${cl}-bn,${cl}-vc" --sim eth2/dencun --sim.limit "eth2-deneb-p2p-blobs-gossip/"
```

## Test Cases
